#!/bin/bash

# Convert environment variables to numbers in base 10
dec_water=$((ibase=$water; WATER))
dec_stir=$((ibase=$stir; STIR))

# Add the two decimal values
sum=$((dec_water + dec_stir))

# Convert the sum to desired base and print
echo "obase=$bestchol; $sum" | bc

